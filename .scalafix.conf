# .scalafix.conf
rules = [
  // Linter: Remove imports, vars e valores não utilizados
  // Essencial para manter o código limpo
  RemoveUnused,

  // Linter: Proíbe certas sintaxes, como 'final' em classes
  // DisableSyntax,

  // Outras regras úteis, se necessário
  // ProcedureSyntax
]

// Exemplo de configuração de regra (opcional)
// DisableSyntax.noFinalize = true